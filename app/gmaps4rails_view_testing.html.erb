
<%= gmaps(:marker     => {:lat => @home.latitude, :lng => @home.longitude },
          :map_options => { :id => "second_map", :center_on_user => true, :zoom => 5 },
          :scripts     => :none ) %>

<%= gmaps(:markers => {:data => @json, :options => { :raw => '{ animation: google.maps.Animation.BOUNCE }' } },
          :map_options => { :raw => '{ disableDefaultUI: true, scrollwheel: false }' }) %> 

<%= gmaps4rails( [{:lat => @home.latitude, :lng => @home.longitude}].to_json ) %>

<%= gmaps(:marker => { data: [{:lat => @home.latitude, :lng => @home.longitude}].to_json } ) %>

